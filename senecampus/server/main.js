import { Meteor } from 'meteor/meteor';

import { Reports } from '../imports/api/reports.js';
import { Buttons } from '../imports/api/buttons.js';


Meteor.startup(() => {
  // code to run on server at startup
});
